# 缓存导致的重复失败

## 现象

容器每次重启都从同一页优化编译开始，重复出现相同错误。

## 已尝试方案

- 只重启容器：损坏的优化中间结果仍被复用。
- 清空整个 `data/workers`：会丢失已完成页和队列进度，代价过大。

## 当前方案

仅清除目标 PDF 的优化、语法修复和编译产物，保留识别缓存、模型缓存和队列状态；从最近一次安全识别结果重新进入优化。

## 验证

重试前确认目标目录中的识别结果仍存在，重试后检查首个 XeLaTeX 错误和 `QUEUE FINISH status: done`。
