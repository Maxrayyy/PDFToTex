# 缺失图片资源

## 现象

模板引用的图片文件不存在，XeLaTeX 报 `File ... not found`，优化阶段退出。

## 已尝试方案

- 直接重启容器：相同输入和缓存会重复失败。
- 删除识别缓存：不能解决模板资源缺失，还会增加重复识别成本。
- 静默删除 `\\includegraphics`：会改变页面结构并隐藏资源问题。

## 当前方案

对缺失资源使用条件分支和占位回退，保留尺寸与版面占位；资源存在时仍使用原图。该修复只作用于缺失文件，不改动正常图片。

## 验证

典型缺失 logo 页面已通过 XeLaTeX 并继续队列。
