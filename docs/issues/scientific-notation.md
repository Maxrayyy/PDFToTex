# 科学计数法与数学模式边界

## 现象

识别结果中的 `1.04\\times10^{7}`、`4\\times10^{7}/a` 等表达式被拆到文本模式和数学模式之间，XeLaTeX 报 `Missing $ inserted`，或上标位置错误。

## 已尝试方案

- 只给孤立的 `\\times`、`\\div` 加数学模式：指数仍留在文本模式，错误未消失。
- 只匹配带空格的 `\\times 10`：OCR 常见的紧凑写法无法命中。
- 对整页或整行强制包 `$...$`：会把单位、字段宏和普通文字误放进数学模式，破坏原有版式。
- 让语法模型重写整段 TeX：模型可能同时改动字段括号和表格边界，产生新的结构错误。

## 当前方案

在确定性规范化阶段识别“数字 + 运算符 + 数字 + 指数/后缀”的最小表达式，统一放入 `\\ensuremath{...}`；只处理生成的 TeX，不修改 PDF。对已在 `\\ensuremath` 内的表达式跳过二次包装，避免嵌套。

## 为什么仍可能失败

模型可能把数学表达式拆成多个字段宏，或输出不完整的大括号；正则无法可靠推断跨宏语义。此时应保留原始识别结果并升级整页视觉识别，不能继续叠加修复。

## 验证

单元测试覆盖紧凑科学计数法、嵌套 `\\ensuremath`、字段后的运算符和上标；最终以双遍 XeLaTeX 编译为准。

## 2026-09-14：May 第 113 页反复 Missing $ inserted

任务：`lexiod-may-202605032-202605027`，PDF 为
`MX-C4081R_20260805_154447.pdf`（127 个源页）。

### 原始证据

`data/workers/MX-C4081R_20260805_154447/.cache/recognition/draft/0ca4119b6ef029242f8368bff20322e0701aa4b7f623c53078a9471972b5512a/113.tex`
在三个结果字段后均含多余的 `$`（原始行 98、182、205）：

```tex
$\times100\%=$
% #HANDWRITTEN: 100\%
\fieldvalue{\handwritten{100\%}}$

确认人/日期：
```

`$\times100\%$` 本身合法。结果字段后的额外 `$` 才会错误地开启数学模式。
最终日志在 `.optimized.tex:20185` 的 `\begin{center}` 报错；报错位置是受害位置，
不能据此断言是前一页、该公式或该环境自身损坏。

### 本次尝试及结果

1. 之前仅将短公式改成 `\ensuremath{...}`：短公式测试通过，但没有移除结果字段的孤立 `$`。
   不足之处是测试只验证替换文本，没有把后续字段、段落及真实编译放进同一用例。
2. 补充整页数学分隔符扫描，仅删除无法配对的字段尾部 `$`：最终坏 TeX 中的一个残留符号被清除，
   完整文档双遍 XeLaTeX 编译通过。但原始第 113 页有三个多余符号，两个可能跨段落错误配对，
   因此仅统计整页奇偶仍不足。
3. 补充真实段落边界、`\par`、源页开始/结束和 `LEXOID_PAGE_COMPLETED` 边界：
   普通物理换行仍可位于公式内，数学模式不可穿越段落或独立源页。
   三个结果字段尾部符号全部被正确识别；合法跨行科学公式保持配对。
4. 发现旧的逐行“单数 `$`”规则会破坏合法跨行公式：
   `$4\times10^{7}/` 与下一行结果字段的结束 `$` 原本有效，旧规则却单独包裹第一行，留下结束符。
   现在逐行修复必须服从整个作用范围的配对结果。
5. 发现 `normalize_math_blank_lines` 会跨源页配对 `$`，进而把正文空行改为 `%`：
   现在使用同一词法边界，遇到源页边界必须清空匹配状态。合法公式内部的空行修复继续保留。
6. 短公式替换此前还会改写注释和 `$$...$$` 展示公式：
   现在只接受词法扫描识别出的单 `$`，注释、转义符号和展示公式不会误命中这条规则。

### 验证与限制

- 红灯测试实际重现了 `Missing $ inserted`、跨行公式被破坏、跨页空行被吞、注释和展示公式被误改。
- 最终新增数学边界校验接入 `validate_latex`，未修复的错误报告 `UNCLOSED_MATH` 和源行号。
- 最终测试镜像完整回归：流水线 365 项、Lexoid 142 项通过，另有 3 个子测试通过；
  一个第三方 Together/Pydantic 弃用警告。`compileall`、`git diff --check` 通过。
  实际 XeLaTeX 编译和渲染产物见 `data/fix/may-math-boundaries/`（与项目平级的 data）。
- `after.tex` 是对原失败的完整优化稿应用修复后的验证文件；双遍编译通过，输出 157 页。
  原始页数为 127，不能把页数或成功编译当作版面合格证明。
- `page113.optimized.tex` 从保存的原始第 113 页识别文本重新规范化、优化、双遍编译，
  `restored-1.png` 显示公式、签名、标题重新分段，消除了数学模式造成的正文串行。
- 视觉检查仍发现原有表格过宽；完整稿几何检查标记输出页 143、146、152、154、155 越界。
  这些验证稿不能直接作为完成的发布结果。
- 验证使用的 XeLaTeX 实际报告为 `TeX Live 2025/dev/Debian`，LaTeX 为 `2024-11-01`；
  不应仅根据 LaTeX 日期称其为 TeX Live 2024。

### 缓存处置教训

不要仅删除最后一个 `$` 后直接发布旧优化稿。旧规范化缓存可能已经把段落空行改成 `%`，
即使可以编译，也会把标题、表格与签名挤在同一行。
必须保留识别草稿，从草稿重新生成受影响页的规范化及后续产物，不能凭猜测把所有 `%` 改回空行。
本次只在 `data/fix/may-math-boundaries/` 保存复现和验证产物，未删除生产识别缓存。
生产镜像尚未更新；May 保持 `Exited (1)`，没有把仍有表格越界的验证稿作为完成产物发布。
